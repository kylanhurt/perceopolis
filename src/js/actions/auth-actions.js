/*import axios from 'axios';

export function login(credentials) {
	return dispatch => {
		return axios.post('http://localhost/api/authenticate/user', credentials).then(resp => {
			const token = resp.data.token;
			localStorage.setItem('jwtToken', token);
		});
	}
}*/